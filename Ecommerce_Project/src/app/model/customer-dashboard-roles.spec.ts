import { CustomerDashboardRoles } from './customer-dashboard-roles';

describe('CustomerDashboardRoles', () => {
  it('should create an instance', () => {
    expect(new CustomerDashboardRoles()).toBeTruthy();
  });
});
